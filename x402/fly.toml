app = "dnp-x402"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  APP_VERSION = "0.1.0"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]
