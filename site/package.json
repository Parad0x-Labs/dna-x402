{
  "name": "dnp-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
