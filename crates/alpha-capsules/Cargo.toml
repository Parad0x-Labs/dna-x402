[package]
name = "alpha-capsules"
version = "0.1.0"
edition = "2021"
description = "Time-locked sealed betting/signal alpha — sell confidence without revealing side"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
