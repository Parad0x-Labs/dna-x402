[package]
name = "agent-flight-recorder"
version = "0.1.0"
edition = "2021"
description = "Cryptographic black-box log for AI money actions — NOT flight surveillance"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
