[package]
name = "receipt-souls"
version = "0.1.0"
edition = "2021"
description = "Unified disposable bearer notes — tips, API access, gifts, predictions. Die forever on spend."

[dependencies]
sha2 = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
