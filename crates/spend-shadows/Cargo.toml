[package]
name = "spend-shadows"
version = "0.1.0"
edition = "2021"
description = "Every real spend emits decoy/delayed/poison/maintenance shadows — controlled ambiguity"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
