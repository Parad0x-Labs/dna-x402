[package]
name = "dark-proof-of-innocence"
version = "0.1.0"
edition = "2021"
description = "Non-membership proof for transaction sets - prove you are NOT in a tainted/sanctioned set"

[dependencies]
sha2  = "0.11"
serde = { version = "1", features = ["derive"] }
