[package]
name = "roadmap-commitments"
version = "0.1.0"
edition = "2021"
description = "Cryptographic roadmap prophecy — commit features as hashes, reveal later to prove pre-knowledge"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
