[package]
name = "dark-private-voting"
version = "0.1.0"
edition = "2021"
description = "Commit-reveal voting with nullifier-based double-vote prevention and private ballot aggregation"

[dependencies]
sha2  = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
