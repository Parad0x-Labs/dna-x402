[package]
name = "dark-private-dutch-auction"
version = "0.1.0"
edition = "2021"
description = "Private Dutch auction — committed bids, only winner's amount revealed"

[dependencies]
sha2  = "0.11"
serde = { version = "1", features = ["derive"] }
