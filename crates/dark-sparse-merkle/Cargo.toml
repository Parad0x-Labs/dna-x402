[package]
name = "dark-sparse-merkle"
version = "0.1.0"
edition = "2021"
description = "Sparse Merkle tree with O(depth) inclusion and non-membership proofs — nullifier-compatible"

[dependencies]
sha2  = "0.11"
serde = { version = "1", features = ["derive"] }
