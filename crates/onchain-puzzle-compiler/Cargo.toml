[package]
name = "onchain-puzzle-compiler"
version = "0.1.0"
edition = "2021"
description = "Compile messages into verifiable Solana ritual tx plans — on-chain cryptographic prophecy"

[dependencies]
sha2 = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
