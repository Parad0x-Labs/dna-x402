[package]
name = "session-note-channel"
version = "0.1.0"
edition = "2021"
description = "Payment channel without a channel PDA — notes + roots + settlement"

[dependencies]
sha2 = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
