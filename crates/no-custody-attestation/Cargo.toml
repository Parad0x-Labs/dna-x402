[package]
name = "no-custody-attestation"
version = "0.1.0"
edition = "2021"
description = "Relayer/agent publishes anti-honeypot capsule — cryptographic proof of not holding keys"

[dependencies]
sha2 = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
