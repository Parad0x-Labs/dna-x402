[package]
name = "dark-timelock-encryption"
version = "0.1.0"
edition = "2021"
description = "Slot-bound time-lock encryption - ciphertext is unreadable until reveal_slot; hash-based VDF simulation"

[dependencies]
sha2  = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
