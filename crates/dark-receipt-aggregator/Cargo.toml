[package]
name = "dark-receipt-aggregator"
version = "0.1.0"
edition = "2021"
description = "Aggregate N x402 receipts into one compact proof for batch verification and private subscription billing"

[dependencies]
sha2  = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
