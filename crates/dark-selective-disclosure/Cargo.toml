[package]
name = "dark-selective-disclosure"
version = "0.1.0"
edition = "2021"
description = "Selective field disclosure for private credentials — reveal any subset of fields, keep the rest committed"

[dependencies]
sha2  = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
