[package]
name = "dark-htlc-receipt"
version = "0.1.0"
edition = "2021"
description = "Hash time-locked receipt primitive for cross-party x402 settlements"

[dependencies]
sha2  = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
