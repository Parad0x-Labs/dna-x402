[package]
name = "dark-compressed-receipt-ledger"
version = "0.1.0"
edition = "2021"

[dependencies]
dark-compression-core = { path = "../dark-compression-core" }
sha2 = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
