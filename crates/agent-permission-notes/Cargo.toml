[package]
name = "agent-permission-notes"
version = "0.1.0"
edition = "2021"
description = "Caveated agent spending rights — user gives cryptographic leash, not custody"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
